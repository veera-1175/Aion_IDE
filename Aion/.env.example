# Groq (free tier) — https://console.groq.com
LLM_PROVIDER=groq
GROQ_API_KEY=gsk-your-groq-key-here
# Best model for code + valid JSON (Groq Structured Outputs strict):
GROQ_MODEL=openai/gpt-oss-20b

# Remove or comment out OpenAI if not using paid API:
# OPENAI_API_KEY=
